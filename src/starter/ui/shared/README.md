# UI Shared Base

This folder contains cross-platform UI baseline assets that are intended to be shared
by Web, Desktop, and Mobile profile implementations.

## Current Baseline

- `design-tokens.css`: starter color, spacing, radius, and typography tokens

## Extension Guidance

- Keep tokens additive and backward compatible where practical.
- Avoid profile-specific assumptions in shared assets.
- Profile-specific assets belong under profile folders.
